= render "devise/shared/header"
.dmRoom
  .dmRoom__container
    - @anotherEntries.each do |e|
      = link_to room_path(e.room.id) do
        .dmRoom__container__list
          = e.user.name
          さんとのDM
  .show-paginate__page
    = paginate(@anotherEntries)